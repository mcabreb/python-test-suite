"""PyTrainer application entry point."""

import sys


def main() -> None:
    """Launch the PyTrainer application."""
    print("PyTrainer v0.1.0 — Python Competition Trainer")
    print("Run with a GUI environment to start a competition session.")
    sys.exit(0)
